# Missed-mutant ratchet for the weekly mutation job (.github/workflows/mutation.yml).
# The job fails if the missed count RISES above this; lower it in a PR when
# you kill survivors. Scope/exclusions: .cargo/mutants.toml.
#
# The number is CI-MEASURED (ubuntu runner) — the enforcing environment is
# the measuring environment (#684 lesson: a macOS-measured 43 lost to CI's
# 44 on the first run; borderline mutants are timing-dependent).
#
# History:
#   44 — first CI measurement (run 31205741102, 2026-08-07)
#   23 — udp_throughput.rs wire-protocol tests killed 21 (#685; CI run
#        31217844497). Remaining bulk: stamp.rs run-loop comparisons (10),
#        udp_throughput's 5 documented TTL-reaper survivors (clock-injection
#        refactor needed), lib.rs network-introspection helpers, main.rs
#        entry points.
23
