#!/usr/bin/env bash
# Stub for cargo.
#
# STUB_CARGO_FAIL   If set to "1", exit 1 (simulates build failure)

if [[ "${STUB_CARGO_FAIL:-}" == "1" ]]; then
    echo "error: could not compile" >&2
    exit 1
fi
echo "Finished release [optimized] target(s) in 0.01s"
exit 0
