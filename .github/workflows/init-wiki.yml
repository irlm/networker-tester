name: Initialize Wiki

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  init-wiki:
    runs-on: ubuntu-latest
    steps:
      - name: Write and push wiki pages
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"
          WIKI_URL="https://x-access-token:${GH_TOKEN}@github.com/${REPO}.wiki.git"
          if git clone "$WIKI_URL" /tmp/wiki 2>/dev/null; then
            echo "Wiki already initialized"
          else
            echo "Bootstrapping wiki"
            mkdir -p /tmp/wiki && cd /tmp/wiki
            git init -b master
            git remote add origin "$WIKI_URL"
            cd /
          fi
          cd /tmp/wiki
          echo "Writing pages..."
          # Pages written via Python heredocs to avoid shell quoting issues
          python3 - << 'PYEOF'
import os

pages = {}

pages["Home.md"] = """# Networker Tester

A cross-platform network diagnostics suite that probes an endpoint across TCP, HTTP/1.1, HTTP/2, HTTP/3, and UDP.

## Quick Install

### macOS and Linux

```bash
curl -fsSL https://gist.githubusercontent.com/irlm/37a1af64b70ef6e58ea117839407f4f9/raw/install.sh | bash -s -- tester
curl -fsSL https://gist.githubusercontent.com/irlm/37a1af64b70ef6e58ea117839407f4f9/raw/install.sh | bash -s -- endpoint
```

### Windows (PowerShell)

```powershell
Invoke-RestMethod 'https://gist.githubusercontent.com/irlm/37a1af64b70ef6e58ea117839407f4f9/raw/install.ps1' | Invoke-Expression
```

## Quick Start

```bash
networker-endpoint
networker-tester --target https://127.0.0.1:8443/health --modes http1,http2,http3 --runs 5 --insecure --output-dir ./output
```

## Wiki Pages

| Page | Contents |
|------|---------|
| [[Installation]] | curl one-liners, local install options, cloud deploy |
| [[Usage]] | All flags, all modes, output formats, config file |
| [[Protocol-Comparison]] | H1 vs H2 vs H3 — what to run and how to read results |
| [[Cloud-Deployment]] | Azure and AWS deployment, auto-shutdown, multi-region |

## What It Measures

| Metric | Why it matters |
|--------|---------------|
| TTFB | Head-of-line blocking sensitivity |
| Throughput / Goodput | Raw vs. true end-to-end transfer rate |
| CPU | QUIC userspace encryption overhead |
| Context switches | I/O concurrency differences |
| Page-load total | Multiplexing impact across protocols |
| TCP kernel stats | RTT, cwnd, retransmits via getsockopt |

## Two Binaries

| Binary | Role |
|--------|------|
| `networker-tester` | CLI client — sends probes, collects metrics, writes reports |
| `networker-endpoint` | Diagnostic server — `/health`, `/download`, `/upload`, `/page`, `/browser-page` |
"""

pages["Installation.md"] = """# Installation

## Quick Install

### macOS and Linux

```bash
curl -fsSL https://gist.githubusercontent.com/irlm/37a1af64b70ef6e58ea117839407f4f9/raw/install.sh | bash -s -- tester
curl -fsSL https://gist.githubusercontent.com/irlm/37a1af64b70ef6e58ea117839407f4f9/raw/install.sh | bash -s -- endpoint
curl -fsSL https://gist.githubusercontent.com/irlm/37a1af64b70ef6e58ea117839407f4f9/raw/install.sh | bash -s -- both
```

### Windows (PowerShell)

```powershell
$GistUrl = 'https://gist.githubusercontent.com/irlm/37a1af64b70ef6e58ea117839407f4f9/raw/install.ps1'
Invoke-RestMethod $GistUrl | Invoke-Expression
```

## Install Modes

| Mode | When used | Speed |
|------|-----------|-------|
| Release | gh CLI installed and authenticated | ~10 seconds |
| Source | No gh CLI; compiles from repo via cargo install | 2-10 minutes |

## Options

```bash
bash install.sh -y tester              # non-interactive
bash install.sh --from-source tester   # force source compile
bash install.sh --skip-rust tester     # skip Rust check
```

## Azure Deployment

```bash
bash install.sh --azure endpoint --region eastus --rg networker-rg --vm-size Standard_B2s
```

## AWS Deployment

```bash
bash install.sh --aws endpoint --aws-region us-east-1 --aws-instance-type t3.small
```

## Build from Source

```bash
git clone https://github.com/irlm/networker-tester.git
cd networker-tester
cargo build --release
```

## Requirements

| Component | Requirement |
|-----------|------------|
| macOS | 11+ |
| Linux | Any modern distribution |
| Windows | 10+ with PowerShell 5.1 or 7+ |
| HTTP/3 | Included by default (UDP 8443 must be open) |
| Browser probe | Chrome/Chromium + `--features browser` |
"""

pages["Usage.md"] = """# Usage

## Basic Usage

```bash
networker-endpoint
networker-tester --target https://127.0.0.1:8443/health --modes http1,http2,http3 --runs 5 --insecure --output-dir ./output
```

## Probe Modes

| Mode | Transport | What it measures |
|------|-----------|-----------------|
| `tcp` | TCP | DNS + TCP connect time |
| `http1` | HTTP/1.1 | DNS, TCP, TLS, TTFB, Total |
| `http2` | HTTP/2 | DNS, TCP, TLS (ALPN), TTFB, Total |
| `http3` | QUIC | QUIC handshake, TTFB, Total |
| `dns` | DNS | Standalone DNS resolution |
| `tls` | TCP + TLS | Cert chain, cipher suite, ALPN |
| `udp` | UDP | RTT min/avg/p95, jitter, loss% |
| `udpdownload` | UDP bulk | NWKT protocol throughput (server->client) |
| `udpupload` | UDP bulk | NWKT protocol throughput (client->server) |
| `download` | HTTP | Throughput MB/s (server->client) |
| `upload` | HTTP | Throughput MB/s (client->server) |
| `webdownload` | HTTP | Download from any URL (no path rewrite) |
| `webupload` | HTTP | Upload to any URL (no path rewrite) |
| `pageload` | HTTP/1.1 | Multi-asset page load, up to 6 connections |
| `pageload2` | HTTP/2 | Multi-asset page load, single multiplexed connection |
| `pageload3` | HTTP/3 | Multi-asset page load, QUIC multiplexed |
| `browser` | Chromium | Real headless browser: Load/DCL/TTFB/bytes/protocols |

`pageload` expands to pageload + pageload2 + pageload3. `browser` expands to browser1 + browser2 + browser3.

## Key Flags

| Flag | Default | Description |
|------|---------|-------------|
| `--target URL` | localhost:8080 | URL to probe (repeat for multi-target) |
| `--modes MODE,...` | http1,http2,udp | Comma-separated probe modes |
| `--runs N` | 3 | Repetitions per mode |
| `--payload-sizes LIST` | — | Sizes for download/upload (e.g. 64k,1m,10m) |
| `--page-assets N` | 20 | Assets per page-load simulation |
| `--page-asset-size SZ` | 10k | Size of each simulated asset |
| `--insecure` | — | Skip TLS certificate verification |
| `--html` | — | Write HTML report |
| `--excel` | — | Write Excel workbook |
| `--json` | — | Write JSON to stdout |
| `--output-dir PATH` | ./output | Output directory |

## Common Examples

```bash
# Protocol latency comparison
networker-tester --target https://127.0.0.1:8443/health --modes http1,http2,http3 --runs 20 --insecure

# Download throughput
networker-tester --target https://127.0.0.1:8443/health --modes download --payload-sizes 1m,10m --runs 5 --insecure

# Page-load comparison
networker-tester --target https://127.0.0.1:8443/health --modes pageload,pageload2,pageload3 --page-assets 30 --runs 10 --insecure

# Multi-target
networker-tester --target http://127.0.0.1:8080/health --target https://<cloud>:8443/health --modes tcp,http1,http2,http3,download --payload-sizes 1m --runs 5 --insecure --output-dir ./output
```

## Configuration File

```json
{
  "targets": ["http://127.0.0.1:8080/health", "https://remote:8443/health"],
  "modes": ["http1", "http2", "http3"],
  "runs": 5,
  "payload_sizes": ["1m"],
  "insecure": true,
  "output_dir": "./output"
}
```

```bash
networker-tester --config my-config.json --runs 10
```

## Endpoint Routes

| Route | Method | Description |
|-------|--------|-------------|
| `/health` | GET | `{"status":"ok"}` |
| `/download?bytes=N` | GET | Returns N bytes with timing headers |
| `/upload` | POST | Drains body, returns byte count |
| `/page` | GET | JSON manifest for pageload probes |
| `/browser-page` | GET | HTML page for browser probes |
"""

pages["Protocol-Comparison.md"] = """# Protocol Comparison: H1 vs H2 vs H3

## Background

| Aspect | HTTP/1.1 | HTTP/2 | HTTP/3 |
|--------|----------|--------|--------|
| Transport | TCP | TCP | QUIC (UDP) |
| Multiplexing | None | Full (one TCP conn) | Full (one QUIC conn) |
| Head-of-line blocking | TCP + HTTP level | TCP level only | None |
| TLS | Kernel-offloaded | Kernel-offloaded | Userspace (per-datagram) |
| Connections for page loads | Up to 6 per host | 1 | 1 |

## Test 1: Request Latency

```bash
networker-tester --target https://127.0.0.1:8443/health --modes http1,http2,http3 --runs 20 --insecure
```

Expected: CPU for HTTP/3 is 2-5x higher (QUIC userspace encryption). HTTP/3 shows `QUIC:Xms` instead of separate DNS/TCP/TLS phases.

## Test 2: Download Throughput

```bash
networker-tester --target https://127.0.0.1:8443/health --modes download --payload-sizes 1m,10m,100m --runs 5 --insecure
```

- **Throughput** = body transfer rate (excludes connection setup)
- **Goodput** = full delivery rate (includes setup); always less than Throughput
- Goodput gap is larger for H3 due to longer QUIC handshake

## Test 3: Page-Load Comparison

```bash
networker-tester --target https://127.0.0.1:8443/health --modes pageload,pageload2,pageload3 --page-assets 30 --page-asset-size 50k --runs 10 --insecure --output-dir ./output
```

| Metric | H1.1 (pageload) | H2 (pageload2) | H3 (pageload3) |
|--------|-----------------|-----------------|-----------------|
| total_ms | Highest | Lower | Similar to H2 |
| connections_opened | Up to 6 | 1 | 1 |
| tls_overhead_ratio | Higher (6 handshakes) | Lower (1 handshake) | Similar to H2 |
| cpu_time_ms | Lowest | Moderate | Highest |

## Test 4: Asset Count Sweep

Find where H2 multiplexing beats H1.1 connection pools (~6-10 assets):

```bash
for N in 5 10 20 40 80; do
  networker-tester --target https://127.0.0.1:8443/health --modes pageload,pageload2 --page-assets $N --page-asset-size 10k --runs 5 --insecure
done
```

## Test 5: Add Real Browser Metrics

```bash
cargo build --release --features browser -p networker-tester
networker-tester --target https://127.0.0.1:8443/health --modes pageload,pageload2,pageload3,browser --page-assets 30 --runs 10 --insecure --output-dir ./output
```

Browser probe captures: Load time, DOMContentLoaded, TTFB, resource count, bytes transferred, per-protocol resource counts.

## Interpreting Results

| What to compare | What it tells you |
|-----------------|-------------------|
| pageload vs pageload2 total_ms | Benefit of H2 multiplexing |
| pageload2 vs pageload3 total_ms | QUIC advantage on lossy links |
| connections_opened in pageload | <= 6; more = more TLS handshake cost |
| cpu_time_ms across protocols | H3 always highest (QUIC userspace) |
| browser Load vs pageload total | Browser render/parse/JS overhead |
"""

pages["Cloud-Deployment.md"] = """# Cloud Deployment

## Overview

The installer provisions a cloud VM, opens ports, deploys the binary, and writes `networker-cloud.json` with the VM IP.

## Azure Deployment

```bash
az login
bash install.sh --azure endpoint
bash install.sh --azure endpoint --region eastus --rg networker-rg --vm-size Standard_B2s
```

| Flag | Default | Description |
|------|---------|-------------|
| `--region` | eastus | Azure region |
| `--rg` | networker-rg-endpoint | Resource group |
| `--vm` | auto-generated | VM name (smart naming, collision detection) |
| `--vm-size` | Standard_B2s | VM size |

### Local vs Azure comparison

```bash
networker-endpoint
networker-tester --target http://127.0.0.1:8080/health --target https://<azure-ip>:8443/health --modes tcp,http1,http2,http3,udp,download --payload-sizes 1m --runs 5 --insecure --output-dir ./output
```

## AWS Deployment

```bash
aws configure
bash install.sh --aws endpoint
bash install.sh --aws endpoint --aws-region us-east-1 --aws-instance-type t3.small
```

| Flag | Default | Description |
|------|---------|-------------|
| `--aws-region` | us-east-1 | AWS region |
| `--aws-instance-type` | t3.small | EC2 instance type |
| `--aws-endpoint-name` | networker-endpoint | EC2 Name tag |

## Auto-Shutdown

Configured at 11 PM EST by default.

```bash
# Azure: change shutdown time
az vm auto-shutdown --resource-group networker-rg --name networker-endpoint-vm --time 2300 --timezone "Eastern Standard Time"

# AWS: edit crontab on instance
# 0 22 * * * /sbin/shutdown -h now
```

## Multi-Region Comparison

```bash
bash install.sh --azure endpoint --region eastus     --rg networker-rg-eastus
bash install.sh --azure endpoint --region uksouth    --rg networker-rg-uksouth
bash install.sh --azure endpoint --region southeastasia --rg networker-rg-sea

networker-tester \\
  --target http://127.0.0.1:8080/health \\
  --target https://<eastus-ip>:8443/health \\
  --target https://<uksouth-ip>:8443/health \\
  --target https://<sea-ip>:8443/health \\
  --modes tcp,http1,http2,http3,download --payload-sizes 1m --runs 10 --insecure --output-dir ./output
```

## Troubleshooting

| Issue | Cause | Fix |
|-------|-------|-----|
| Health check times out | Firewall propagation delay | Wait 30-60 s and retry |
| HTTP/3 not working | UDP 8443 blocked | Check NSG / security group rules |
| No pre-built binary | Source-build fallback | Normal; installer compiles on VM (~5 min) |
"""

os.chdir('/tmp/wiki')
for filename, content in pages.items():
    with open(filename, 'w') as f:
        f.write(content)
    print(f"Written: {filename}")
PYEOF
          ls -la /tmp/wiki/
          git add .
          git status
          git commit -m "Add wiki pages: Home, Installation, Usage, Protocol-Comparison, Cloud-Deployment" || echo "Nothing to commit"
          git push origin master --force || git push origin HEAD:master --force

      - name: Delete this workflow file
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
        run: |
          SHA=$(gh api "/repos/$REPO/contents/.github/workflows/init-wiki.yml" --jq '.sha' 2>/dev/null || echo "")
          if [ -n "$SHA" ]; then
            gh api --method DELETE "/repos/$REPO/contents/.github/workflows/init-wiki.yml" \
              -f message="Remove one-time wiki init workflow" \
              -f sha="$SHA" && echo "Workflow self-deleted" || echo "Could not self-delete"
          fi
