# Shared probe token for all five LagHound sample services.
# The LagHound routes (/laghound/*) are invisible (bare 404) without it.
# Copy to `.env` (docker compose auto-loads it) or export it in your shell.
#
# Contract requires >= 16 bytes; the demo default below is exactly that.
LAGHOUND_TOKEN=demo-token-laghound
