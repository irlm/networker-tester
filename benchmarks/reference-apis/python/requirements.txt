# Runtime identity is uvicorn (API-SPEC.md §3; audit F12 resolved wave 2):
# the Dockerfile, deploy.sh, and ci/run-language.sh all launch uvicorn.
# No HTTP/3, so no Alt-Svc advertisement anywhere.
starlette>=0.46,<1
uvicorn>=0.30,<1
